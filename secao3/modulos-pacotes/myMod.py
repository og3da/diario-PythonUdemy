def multiply(v1,v2):
    valor=v1*v2 
    return valor

def div(v1,v2):
    valor=v1/v2 
    return valor

if __name__ == '__main__': # no caso se o modulo for executado no proprio arquivo dele
    print("sendo executado no proprio arquivo isso aparecera, isso nao ira aparecer no arquivo que o modulo foi importado")